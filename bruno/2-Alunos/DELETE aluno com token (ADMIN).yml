info:
  name: DELETE usuário com token (ADMIN)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/usuarios/{{usuarioCriadoId}}"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 204", function() {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
