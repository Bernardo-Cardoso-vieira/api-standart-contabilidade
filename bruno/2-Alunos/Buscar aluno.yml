info:
  name: Buscar usuário
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/usuarios/{{usuarioCriadoId}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("não deve retornar senhaHash", function() {
          expect(res.getBody()).to.not.have.property('senhaHash');
        });

        test("deve retornar o usuário correto", function() {
          expect(res.getBody().id).to.equal(Number(bru.getVar('usuarioCriadoId')));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
