info:
  name: Listar serviços
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/servicos"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar serviços publicamente", function() {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
